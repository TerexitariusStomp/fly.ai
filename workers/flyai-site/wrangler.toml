name = "flyai-site"
compatibility_date = "2026-09-18"
account_id = "23738a036108482feac164d7eee10a30"

# widespread.fyi zone lives on the old account — can't bind it here.
# Site is reachable at flyai-site.symbient.workers.dev until a zone on
# this account is pointed at it.
workers_dev = true

# Static site: vendored fly.ai pages + flybook SPA (built into ./public by build.sh)
[assets]
directory = "./public"
