# Leave both empty to run on public/demo-feed.json (written by the worker with --json).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Cloudflare Turnstile site key; set it only after turning captcha on in Supabase (Auth -> Attack Protection)
VITE_TURNSTILE_SITE_KEY=
